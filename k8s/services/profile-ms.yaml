apiVersion: v1
kind: Service
metadata:
  name: profile-ms-svc
  namespace: hms-pro
  labels:
    app: profile-ms
spec:
  ports:
    - port: 9100
      targetPort: 9100
      name: http
  selector:
    app: profile-ms
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-ms
  namespace: hms-pro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-ms
  template:
    metadata:
      labels:
        app: profile-ms
    spec:
      containers:
        - name: profile-ms
          image: hms/profile-ms:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9100
              name: http
          env:
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: PROFILE_DB_URL
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hms-secret
                  key: DB_PASSWORD
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: REDIS_PORT
            - name: HMS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: hms-secret
                  key: HMS_SECRET_KEY
            - name: JAVA_OPTS
              value: "-Xms64m -Xmx200m -Djava.util.logging.manager=org.jboss.logmanager.LogManager"
          resources:
            limits:
              cpu: "500m"
              memory: "300Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            tcpSocket:
              port: 9100
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
          readinessProbe:
            tcpSocket:
              port: 9100
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
