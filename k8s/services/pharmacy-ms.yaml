apiVersion: v1
kind: Service
metadata:
  name: pharmacy-ms-svc
  namespace: hms-pro
  labels:
    app: pharmacy-ms
spec:
  ports:
    - port: 9300
      targetPort: 9300
      name: http
  selector:
    app: pharmacy-ms
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pharmacy-ms
  namespace: hms-pro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pharmacy-ms
  template:
    metadata:
      labels:
        app: pharmacy-ms
    spec:
      containers:
        - name: pharmacy-ms
          image: hms/pharmacy-ms:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9300
              name: http
          env:
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: PHARMACY_DB_URL
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hms-secret
                  key: DB_PASSWORD
            - name: PAYMENTMS_URL
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: PAYMENTMS_URL
            - name: HMS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: hms-secret
                  key: HMS_SECRET_KEY
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: JAVA_OPTS
              value: "-Xms64m -Xmx200m -Djava.util.logging.manager=org.jboss.logmanager.LogManager"
          resources:
            limits:
              cpu: "500m"
              memory: "300Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            tcpSocket:
              port: 9300
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
          readinessProbe:
            tcpSocket:
              port: 9300
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
