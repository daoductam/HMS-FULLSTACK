apiVersion: v1
kind: Service
metadata:
  name: user-ms-svc
  namespace: hms-pro
  labels:
    app: user-ms
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: user-ms
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-ms
  namespace: hms-pro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-ms
  template:
    metadata:
      labels:
        app: user-ms
    spec:
      containers:
        - name: user-ms
          image: hms/user-ms:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: USER_DB_URL
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hms-secret
                  key: DB_PASSWORD
            - name: PROFILEMS_URL
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: PROFILEMS_URL
            - name: CORS_ALLOWED_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: CORS_ALLOWED_ORIGINS
            - name: JAVA_OPTS
              value: "-Xms64m -Xmx200m -Djava.util.logging.manager=org.jboss.logmanager.LogManager"
          resources:
            limits:
              cpu: "500m"
              memory: "300Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 8
